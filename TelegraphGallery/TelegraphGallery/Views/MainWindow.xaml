<Window
    x:Class="TelegraphGallery.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewModels="clr-namespace:TelegraphGallery.ViewModels"
    Title="{Binding Title}"
    Width="1100"
    Height="700"
    MinWidth="600"
    MinHeight="400"
    d:DataContext="{d:DesignInstance viewModels:MainWindowViewModel}"
    prism:ViewModelLocator.AutoWireViewModel="True"
    AllowDrop="True"
    DragOver="MainWindow_OnDragOver"
    Drop="MainWindow_OnDrop"
    mc:Ignorable="d">
    <Window.Icon>
        <StaticResource ResourceKey="AppIcon" />
    </Window.Icon>
    <DockPanel>
        <ContentControl prism:RegionManager.RegionName="ToolbarRegion" DockPanel.Dock="Top" />
        <ContentControl prism:RegionManager.RegionName="StatusBarRegion" DockPanel.Dock="Bottom" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ContentControl Grid.Column="0" prism:RegionManager.RegionName="GalleryRegion" />
            <ContentControl Grid.Column="1" prism:RegionManager.RegionName="ConfigRegion" />
        </Grid>
    </DockPanel>
</Window>
