<UserControl
    x:Class="TelegraphGallery.Views.GalleryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:TelegraphGallery.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:TelegraphGallery.Models"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewModels="clr-namespace:TelegraphGallery.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:GalleryViewModel}"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
        <converters:BoolToOpacityConverter x:Key="BoolToOpacityConverter" />
        <converters:DuplicateGroupToBrushConverter x:Key="DuplicateGroupToBrushConverter" />
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <ItemsControl
            dd:DragDrop.DropHandler="{Binding}"
            dd:DragDrop.IsDragSource="True"
            dd:DragDrop.IsDropTarget="True"
            ItemsSource="{Binding Items}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border
                        Margin="4"
                        Padding="4"
                        d:DataContext="{d:DesignInstance models:GalleryItem}"
                        Background="{Binding DuplicateGroupId, Converter={StaticResource DuplicateGroupToBrushConverter}}"
                        CornerRadius="4"
                        MouseLeftButtonDown="Thumbnail_MouseLeftButtonDown"
                        Opacity="{Binding IsExcluded, Converter={StaticResource BoolToOpacityConverter}}"
                        Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ItemsControl}}">
                        <Border.ContextMenu>
                            <ContextMenu>
                                <MenuItem
                                    Command="{Binding PlacementTarget.Tag.ToggleExcludeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                    CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                    Header="Toggle Exclude" />
                            </ContextMenu>
                        </Border.ContextMenu>
                        <StackPanel Width="{Binding DataContext.ThumbnailDisplaySize, RelativeSource={RelativeSource AncestorType=ItemsControl}, FallbackValue=150}">
                            <Grid Height="{Binding DataContext.ThumbnailDisplaySize, RelativeSource={RelativeSource AncestorType=ItemsControl}, FallbackValue=150}">
                                <Image
                                    RenderOptions.BitmapScalingMode="HighQuality"
                                    Source="{Binding Thumbnail}"
                                    Stretch="Uniform" />
                                <!--  Loading spinner (visible while thumbnail is being generated)  -->
                                <Grid Visibility="{Binding IsLoadingThumbnail, Converter={StaticResource BoolToVisConverter}}">
                                    <Ellipse
                                        Width="32"
                                        Height="32"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        RenderTransformOrigin="0.5,0.5"
                                        Stroke="#888"
                                        StrokeDashArray="2 2"
                                        StrokeThickness="2">
                                        <Ellipse.RenderTransform>
                                            <RotateTransform x:Name="SpinTransform" />
                                        </Ellipse.RenderTransform>
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsLoadingThumbnail}" Value="True">
                                                        <DataTrigger.EnterActions>
                                                            <BeginStoryboard x:Name="SpinStoryboard">
                                                                <Storyboard>
                                                                    <DoubleAnimation
                                                                        RepeatBehavior="Forever"
                                                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                                        From="0"
                                                                        To="360"
                                                                        Duration="0:0:1" />
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.EnterActions>
                                                        <DataTrigger.ExitActions>
                                                            <StopStoryboard BeginStoryboardName="SpinStoryboard" />
                                                        </DataTrigger.ExitActions>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                </Grid>
                                <!--  Video placeholder  -->
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="20"
                                    FontWeight="Bold"
                                    Foreground="White"
                                    Text="VIDEO"
                                    Visibility="{Binding IsVideo, Converter={StaticResource BoolToVisConverter}}" />
                                <!--  Excluded overlay  -->
                                <TextBlock
                                    Margin="0,0,0,4"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Bottom"
                                    FontSize="14"
                                    FontWeight="Bold"
                                    Foreground="Red"
                                    Text="EXCLUDED"
                                    Visibility="{Binding IsExcluded, Converter={StaticResource BoolToVisConverter}}" />
                            </Grid>
                            <TextBlock
                                Margin="0,2,0,0"
                                HorizontalAlignment="Center"
                                FontSize="11"
                                Text="{Binding FileName}"
                                TextTrimming="CharacterEllipsis" />
                            <TextBlock
                                HorizontalAlignment="Center"
                                FontSize="9"
                                Foreground="Gray"
                                Text="{Binding SubFolder}"
                                TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
